%h2 #{@item.name}

#graph.lines
  %svg{ :style => 'height: 200px' }

%table.records
  - @item.records.each do |record|
    %tr
      %td #{record.amount}
      %td #{record.created_at}

%script{ :type => 'text/javascript' }
  graph_data = { values: [#{@item.records.map{ |record| record.amount if !record.amount.nil? }.join(',')}] }